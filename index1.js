const {
    WAConnection,
    MessageType,
    Presence,
    Mimetype,
    GroupSettingChange

		if (messagesC.includes("bostão")){
			client.updatePresence(from, Presence.composing)
			reply("Vsfd")
	}
	
					}
	})
}
starts()
