const {
    WAConnection,
    MessageType,
    Presence,
    Mimetype,
    GroupSettingChange
} = require('@adiwajshing/baileys')
		if (messagesC.includes("hmmm")){
			client.updatePresence(from, Presence.composing)
			reply("ai")
	}
		
		if (messagesC.includes("kakaka")){
			client.updatePresence(from, Presence.composing)
			reply("Tai")
	}
		
		if (messagesC.includes("sd")){
			client.updatePresence(from, Presence.composing)
			reply("i")
	}
		})
}
starts()
